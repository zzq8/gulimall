spring:
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
      #config必须写到bootstrap.yaml才有效
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yaml
        shared-dataids: common.yaml